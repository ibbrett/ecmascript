---
title: 'splice()'
pubDate: 'Dec 01 1999'
header_title: 'splice() - add item at index'
header_long_title: 'Array.prototype.splice()'
syntax: 'splice(start [, deleteCount [, item1-itemN]])'
parent: 'splice'
what: 'add items to array'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}

- to add item:
  - to the beginning of array use [unshift()](../unshift/)
  - to the end of array use [push()](../push/)

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = ['apple', 'cantaloupe'];
console.log('initial list', list);
// Expected output: ['apple', 'cantaloupe']

// add an item at a particular index
const returnValue = list.splice(1, 0, 'banana');

console.log('add item to list', list);
// Expected output: ['apple', 'banana', 'cantaloupe']

console.log('returnValue', returnValue);
// Expected output: []
```
</div>
