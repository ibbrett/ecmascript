---
title: 'of()'
pubDate: 'Jun 01 2015'
header_title: 'of()'
header_long_title: 'Array.of()'
syntax: 'of([item1, ...[[itemN]])'
parent: ''
what: 'static method, creates a new Array instance from supplied arguments'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}  

- simplifies the process of creating arrays with specified elements
- create arrays with a dynamic number of elements

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const getArrayFromArgs = ( ...args ) => Array.of( ...args );

const dynamicArray = getArrayFromArgs( true, 'carpe diem', 42, {} );

console.log(dynamicArray);
// Expected Output: [true, 'carpe diem', 42, {…}]
```
</div>
