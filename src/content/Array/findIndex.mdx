---
title: 'findIndex()'
pubDate: 'Jun 01 2015'
header_title: 'findIndex()'
header_long_title: 'Array.prototype.findIndex()'
syntax: 'findIndex(callbackFn [, thisArg])'
parent: ''
what: 'returns the index of the first item satisfying the testing function, iterates the array in ascending order'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}  

- similar to:
  - [find()](../../Array/find/)
  - [findLast()](../../Array/findlast/)

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = [1, 3, 5, 7, 9, 5, 3, 1];
console.log('list', list);
// Expected Output: [1, 3, 5, 7, 9, 5, 3, 1]

console.log('find first num larger than 5', list.findIndex( item => item > 5 ));
// Expected Output: 3
```
</div>
