---
title: 'findLast()'
pubDate: 'Jun 01 2023'
header_title: 'findLast()'
header_long_title: 'Array.prototype.findLast()'
syntax: 'findLast(callbackFn [, thisArg])'
parent: ''
what: 'returns the value of the first item satisfying the testing function, iterates the array in reverse order'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}  

- similar to:
  - [find()](../../Array/find/)
  - [findIndex()](../../Array/findindex/)

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = [1, 3, 5, 7, 9, 5, 3, 1];
console.log('list', list);
// Expected Output: [1, 3, 5, 7, 9, 5, 3, 1]

console.log('find first num larger than 5, search in reverse order', list.findLast( item => item > 5 ));
// Expected Output: 9
```
</div>
