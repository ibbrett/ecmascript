---
title: 'map()'
pubDate: 'Dec 03 2009'
header_title: 'map()'
header_long_title: 'Array.prototype.map()'
syntax: 'map(callbackFn [, thisArg])'
parent: ''
what: 'creates a new array populated with function call results on each item'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = ['apple', 'cherry', 'pumpkin', 'pecan', 'blueberry', 'peach'];
console.log('list', list);
// Expected output: ['apple', 'cherry', 'pumpkin', 'pecan', 'blueberry', 'peach']

console.log('pies', list.map( item => `${item} pie`));
// Expected output: ['apple pie', 'cherry pie', 'pumpkin pie', 'pecan pie', 'blueberry pie', 'peach pie']
```
</div>
