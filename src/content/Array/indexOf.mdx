---
title: 'indexOf()'
pubDate: 'Dec 03 2009'
header_title: 'indexOf()'
header_long_title: 'Array.prototype.indexOf()'
syntax: 'indexOf(searchElement, [fromIndex])'
parent: ''
what: 'returns the first found item index, returns -1 if it is not found'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = ['apple', 'blueberry', 'cherry', 'apple'];
console.log('all items', list);
// Expected output: 

let searchItem = 'apple';
console.log(`index for ${searchItem} in list`, list.indexOf(searchItem));
// Expected output: 3

searchItem = 'banana';
console.log(`index for ${searchItem} in list`, list.indexOf(searchItem));
// Expected output: -1
```
</div>
