---
title: 'forEach()'
pubDate: 'Dec 03 2009'
header_title: 'forEach()'
header_long_title: 'Array.prototype.forEach()'
syntax: 'forEach(callbackFn [, thisArg])'
parent: ''
what: 'executes a provided function for each array item'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = ['apple', 'banana', 'cantaloupe'];

list.forEach( item => console.log(item) );

// Expected output: 
//    apple
//    banana
//    cantaloupe
```
</div>
