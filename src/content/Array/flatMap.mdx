---
title: 'flatMap()'
pubDate: 'Jun 01 2019'
header_title: 'flatMap()'
header_long_title: 'Array.prototype.flatMap()'
syntax: 'flatMap(callbackFn [, thisArg])'
parent: ''
what: 'identical to a map() followed by a flat() of depth 1'
---
import CopyToClipboard from '../../components/CopyToClipboard.astro'

{frontmatter.what}  

<div class="copy-to-clipboard-wrap" id="code-sample-01">
<CopyToClipboard id="code-sample-01"></CopyToClipboard>
```js
const list = ['e pluribus unum', 'carpe diem', 'memento mori', 'quid pro quo'];
console.log('array of phrases', list);

// split phrases into words
const flatList = list.flatMap((item) => item.split(' '));
console.log('flat list', flatList);
```
</div>
